# nodeJS_Playground
Node JS Playground

It's a simple Playground.

For Windows 11

für NPM install muss man die ExecutionPolicy mit:
	Set-ExecutionPolicy RemoteSigned
setzen.


git
creating new Branche
- git checkout -b branch_name

create an commit
- git commit -m "description"

push all commits
- git push

show all branches
- git branch -a

switch to other branches
- git switch branch_name

delete an Branch
- git branch -d branch_name